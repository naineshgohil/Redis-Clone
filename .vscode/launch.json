{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Redis-Clone",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/zig-out/bin/redis_clone",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "zig build",
      "sourceLanguages": ["zig"],
      "terminal": "integrated"
    }
  ]
}
